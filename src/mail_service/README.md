Mail Service - Система рассылки

Модуль для автоматической рассылки email писем участникам Новогоднего Квеста с интеграцией Telegram бота и базой данных.

📁 Структура модуля
src/mail_service/
├── __init__.py
├── check_system.py          # Проверка системы
├── config.py               # Конфигурация SMTP
├── email_main.py           # Главный скрипт
├── email_sender.py         # Отправка писем
├── email_templates.py      # Шаблоны писем
├── scheduler.py            # Планировщик
├── utils.py                # Утилиты для БД
└── mail_service_*.log      # Логи рассылки

🚀 Быстрый старт
1. Настройка конфигурации
Добавьте в файл .env в корне проекта:

ENV
# SMTP настройки
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_EMAIL=your.email@gmail.com
SMTP_PASSWORD=your_app_password

# Telegram бот
TELEGRAM_BOT_USERNAME=your_bot_username
2. Проверка системы
BASH
cd src/mail_service
python check_system.py
3. Запуск системы
BASH
# Интерактивный режим
python email_main.py

# Автоматический режим
python email_main.py --auto

# Немедленная рассылка
python email_main.py --immediate --template universal_link
📧 Основные возможности
Типы рассылок:
🤖 Universal Link - приветственное письмо с Telegram ссылкой
🎉 Completion - письмо о завершении квеста
🧪 Test - тестовое письмо


Шаблоны писем:
PYTHON
# HTML + текстовая версии
# Персонализированные обращения
# Ссылки на Telegram бота
# Адаптивный дизайн
Логика рассылки:
Автоматический отбор получателей из БД
Интервал проверки каждые 5 минут
Защита от дублирования (20 часов между отправками)
Обработка ошибок и повторные попытки


🔧 Технические особенности
Архитектура:
TEXT
База данных → Получатели → SMTP сервер → Email
     ↓              ↓           ↓         ↓
  SQLite       Фильтрация   Отправка   Доставка


Ключевые классы:

EmailSender - отправка писем:
PYTHON
class EmailSender:
    def test_connection(self) -> bool
    def send_email(self, to_email, subject, html_content, text_content) -> bool
    def send_bulk_emails(self, template_name) -> dict

EmailScheduler - планировщик:
PYTHON
class EmailScheduler:
    async def start_frequent_scheduler(self, interval_minutes, template_name)
    def stop_scheduler(self)
    def is_running(self) -> bool

EmailTemplates - шаблоны:
PYTHON
class EmailTemplates:
    def universal_link_template(self, recipient) -> tuple
    def completion_template(self, recipient) -> tuple
    def welcome_template(self, recipient) -> tuple

📊 Интеграция с базой данных
Структура данных:
SQL
manual_upload:
  participant_id, last_name, first_name, email, stage_id

link_generation:
  participant_id, universal_link, status, mailing_date
Логика отбора получателей:
SQL
SELECT * FROM manual_upload mu
JOIN link_generation lg ON mu.participant_id = lg.participant_id
WHERE 
  mu.email IS NOT NULL
  AND lg.status = 1
  AND lg.universal_link IS NOT NULL
  AND (
    lg.mailing_date IS NULL 
    OR datetime(lg.mailing_date) < datetime('now', '-20 hours')
  )
Генерация Telegram ссылок:
PYTHON
def create_telegram_link(token: str) -> str:
    bot_username = get_bot_username()  # Из .env
    return f"https://t.me/{bot_username}?start={token}"


⚙️ Конфигурация SMTP
Поддерживаемые провайдеры:
Gmail:
ENV
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_EMAIL=your.email@gmail.com
SMTP_PASSWORD=app_password  # Пароль приложения!
Yandex:
ENV
SMTP_SERVER=smtp.yandex.ru
SMTP_PORT=465
SMTP_EMAIL=your.login@yandex.ru
SMTP_PASSWORD=app_password
Mail.ru:
ENV
SMTP_SERVER=smtp.mail.ru
SMTP_PORT=465
SMTP_EMAIL=your.email@mail.ru
SMTP_PASSWORD=app_password


Важные замечания:
Используйте пароли приложений, не основные пароли
Порт 465 для SSL, порт 587 для STARTTLS
Проверяйте настройки безопасности почтового ящика

🎯 Шаблоны писем
Universal Link Template:
Тема: "🎄 Добро пожаловать в Новогодний Квест, {Имя}!"
Содержание:
Приветствие и описание квеста
Кнопка "Начать Квест в Telegram"
Инструкция по началу квеста
Персональная ссылка
Поддержка для пользователей без Telegram
Completion Template:
Тема: "🎉 Поздравляем с завершением Новогоднего Квеста, {Имя}!"
Содержание:
Поздравления с завершением
Благодарность за участие
Информация о результатах
Призы и награды
Пожелания на Новый год


🔄 Планировщик
Режимы работы:
Частая проверка (рекомендуется):
PYTHON
# Проверка каждые 5 минут
await scheduler.start_frequent_scheduler(5, 'universal_link')
Ежедневная рассылка (устарело):
PYTHON
# В 09:00 каждый день
await scheduler.start_daily_scheduler("09:00", 'universal_link')
Логика планировщика:
Проверка БД на новых получателей
Фильтрация по статусу и времени
Отправка писем с паузами
Обновление mailing_date
Ожидание следующей проверки


🛠️ Команды управления
Через скрипт:
BASH
# Тестирование SMTP
python email_main.py --test

# Немедленная рассылка
python email_main.py --immediate --template universal_link

# Автоматический режим
python email_main.py --auto

# С настройкой интервала
python email_main.py --interval 10 --template completion
Через Telegram бот:
BASH
/mail_status          # Статус системы
/send_mail           # Немедленная рассылка


📈 Мониторинг и логирование
Логи:
Консольный вывод в реальном времени
Файлы логов с датами: mail_service_20241201.log
Детальная информация о каждой отправке
Статистика успешных/неудачных отправок

Формат логов:
TEXT
2024-12-01 10:30:15 - mail_service_main - INFO - ✅ Письмо отправлено: user@example.com
2024-12-01 10:30:17 - mail_service_main - ERROR - ❌ Ошибка SMTP для user2@example.com
🚨 Обработка ошибок
Типичные ошибки:
❌ SMTP Authentication Error - неверные учетные данные
❌ SMTP Data Error - лимиты отправки, пауза 30 секунд
❌ Connection Timeout - проблемы с сетью
❌ Invalid Email - некорректный адрес
Стратегии восстановления:
Повторные попытки с экспоненциальной задержкой
Пропуск проблемных email адресов
Логирование ошибок для последующего анализа
Уведомления администратору


🔒 Безопасность
Защита данных:
Пароли приложений вместо основных паролей
SSL/TLS шифрование соединений
Ограничение доступа к .env файлу
Безопасное хранение токенов в БД
Анти-спам меры:
Персонализированные письма
Опция отписки в шаблонах
Разумные лимиты отправки
Валидация email адресов


📋 Требования
Зависимости:
PYTHON
# Стандартная библиотека Python
smtplib, email, sqlite3, asyncio, logging
Переменные окружения:
SMTP_SERVER - SMTP сервер
SMTP_PORT - порт (465/587)
SMTP_EMAIL - email отправителя
SMTP_PASSWORD - пароль приложения
TELEGRAM_BOT_USERNAME - имя бота для ссылок
Права доступа:
Чтение/запись в базу данных
Сетевой доступ к SMTP серверу
Чтение .env файла
Запись логов


Статус модуля: 🟢 Активный
Интеграция с ботом: ✅ Полная
Поддержка SMTP: ✅ Многопровайдерская
Автоматизация: ✅ Планировщик